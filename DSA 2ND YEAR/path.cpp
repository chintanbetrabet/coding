#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    
    int n;
    cin>>n;
    char ar[n][n];
    vector<int>nodes[n];
    for(int i=0;i<n;i++)
        {
        for(int j=0;j<n;j++)
            cin>>ar[i][j];
    }
    for(i=0;i<n*n;i++)
    {
	    int x=i/n;
	    int y=i%n;
	    int off=-1;
	    while(pos>=0&&)
	    {
		    if(ar[(i+off)/n][
		 }
	 }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}