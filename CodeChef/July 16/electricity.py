import numpy as np
def find_nearest(array,value):
    
A=list({ 1,3,4,6,8 })
A=np.array(A)
print find_nearest(A,5)
'''
t=input()
for test in range(t):
    n=input()
    binary=map(int,raw_input().split(' '))
    x=map(int,raw_input().split(' '))
    i=0
    total=0
    while binary[i]==0:
        i++
    total=x[i]-x[0]
    
'''
